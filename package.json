{
  "name": "signum-cli",
  "version": "0.1.0-b.2",
  "private": true,
  "type": "module",
  "description": "Command-line client for Signum Framework applications",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pajoma/signum-cli.git"
  },
  "bugs": {
    "url": "https://github.com/pajoma/signum-cli/issues"
  },
  "homepage": "https://github.com/pajoma/signum-cli#readme",
  "license": "UNLICENSED",
  "scripts": {
    "build": "bun build --compile --minify --outfile dist/signum src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "npm run typecheck && npm test",
    "build:all": "bun scripts/build-all.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.1",
    "bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
